# Requirements for evaluation scripts

# Core dependencies
pandas>=2.0.0

# LogAI - log anomaly detection toolkit (Salesforce)
# Requires Python 3.10 -- known issues with 3.11+ (numba, dataclass compat)
# Install in a separate venv if your default Python is 3.11+
# After install: python -m nltk.downloader punkt
logai>=0.1.5

# Elasticsearch client - log ingestion for Elastic Observability
elasticsearch>=8.0.0
